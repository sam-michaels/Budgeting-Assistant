@inherits LayoutComponentBase
@layout BudgetAssistant.Web.Components.Layout.MainLayout

@* Sign-in, registration and recovery are single-column, narrow, and centred — the
   same surfaces as the rest of the app so arriving at the product does not feel
   like leaving one. Manage pages override this with ManageLayout. *@
<div class="auth">
    @Body
</div>
